# Agent Entry

本项目的详细协作规则写在 [`agent.md`](./agent.md)。

所有后续 Agent 在修改代码前，应先阅读 `agent.md` 和 `docs/sop/README.md`，并在新增核心业务流程时同步更新 SOP。

APM_RULES {

## 工作区保护

- 当前工作区可能包含用户或其他 Agent 的未提交改动。开始修改前先检查状态，只处理当前任务范围内的文件，不回退、覆盖或清理无关改动。
- 禁止使用 `git reset --hard`、`git checkout --` 等破坏性命令；发现重叠修改时先理解并兼容现状。
- `.vercel/`、`.env*`、认证 token、账户凭证和其他私密配置不得提交或写入项目文档。

## 版本控制

- APM 并行任务从 `main` 的已确认基线创建隔离分支，分支命名为 `codex/<type>/<short-description>`。
- 提交信息使用 `<type>: <description>`，可用类型为 `feat`、`fix`、`refactor`、`test`、`docs`、`chore`。

## 实施与测试

- 修改业务行为或修复缺陷时，先建立能够复现预期差异的测试，再实施最小修复。
- 保持既有页面、字段、mock 数据和业务结果稳定；除非任务明确要求，不进行视觉重设计、状态管理替换或无关重构。
- 领域规则放在共享 domain 模块中，页面不得复制权限、状态机、脱敏或交付校验逻辑。
- 修改前端页面或路由时，验证桌面和移动端布局、直接路由访问、关键交互和浏览器控制台健康。
- 截图、trace、测试报告和临时诊断文件放在仓库外或明确忽略的目录，不提交到 Git。
- 完成任务前至少运行 `pnpm test`、`pnpm build`、`pnpm lint` 和 `git diff --check`；不能运行的检查必须在报告中说明原因和剩余风险。

## 文档与发布

- 业务流程或操作规则变化时，同步更新对应 `docs/sop/` 文档；纯内部重构不应虚构流程变化。
- 发布检查和交接报告必须反映当前真实配置、测试数量、构建结果、线上地址和剩余风险，不保留已失效描述。
- 使用 Vercel 时通过临时 CLI 调用，避免要求全局安装；不得在输出或文档中泄露认证信息。
- Production 发布前必须暂停，向用户展示 Preview 验收结果，并取得明确批准。

} //APM_RULES
